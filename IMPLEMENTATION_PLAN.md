# Implementation Plan

Refreshed 2026-07-03 from the adopted GTM strategy ([docs/nordic-gtm-strategy.md](docs/nordic-gtm-strategy.md)), the spec weave that implemented it (Specs 21–23 plus amendments to 01/04/07/13/15/17/18), and a six-agent spec-to-source verification fan-out over `internal/*`, `apps/*`, `cmd/*`. This is an execution queue, not a changelog; every open item was confirmed against current source with file:line evidence. The previous plan is archived at [docs/archive/IMPLEMENTATION_PLAN-2026-06-09.md](docs/archive/IMPLEMENTATION_PLAN-2026-06-09.md); its still-open items are carried forward below, reprioritized under the GTM lens.

Priority order: **Iceland localization first** (the stated first goal), then the rest of GTM Phase 0 (ISK pricing, Icelandic emails), then re-spin (the GTM centerpiece), then the landing trust layer (founder-led content, deliberately queued after re-spin), then carried-forward correctness, hardening, and cleanup.

**Amended 2026-07-12:** with P0/P1 effectively shipped, the re-spin fidelity fixes below jump to the head of the queue. The 2026-07-12 production QA ([respin-QA.md](respin-QA.md)) showed a "succeeded" re-spin shipping with no phone/email/address, none of the source's six services, and literal placeholder testimonial copy — the opposite of the "floored by the re-spin" demo the GTM strategy depends on. These fixes gate the 50-site Icelandic QA loop.

**Amended 2026-07-13:** follow-up QA on the same source (mysewerguys.squarespace.com) after the P2a integrity fixes: logo and image extraction now work, but the brand color was missed (Squarespace declares its palette as raw HSL triples in an *external* stylesheet — `--accent-hsl: 324.37,79.12%,51.18%`, a vivid magenta — while our extractor reads only inline `<style>`/`style=` and parses only hex/`rgb()`), and the generated hero was flat next to the source's text-only, type-led original. Three new brand/hero fidelity items join the head of P2a, and the headless capture service is promoted out of Deferred into a new P2b with a decided architecture (ADR 0004; Specs 02/04/07/11/12/13/21 updated 2026-07-13). Same-day second amendment: re-spin QA also showed we flatten the source's list-shaped content (services, team/people) into static blocks even though the entire collections substrate ships in the product — a new top-priority P2a item seeds collections + entries from extraction into generation via a `GenerateInput.SeedCollections` seam that the P5 initial-generation item will reuse.

## P0 — Icelandic Landing (Spec 22)

The public marketing surface is ~35 hardcoded English strings on `index.tsx` plus ~16 on `login.tsx`/`restore.tsx`, with zero i18n infrastructure and `<html lang="en">` hardcoded at `apps/web/src/routes/__root.tsx:103`.

- [~] Write the Icelandic landing copy (founder task; prerequisite for everything below). ~50 strings across landing, login, restore: hero, "How it spins", "Made for", prompt chips, CTAs, errors, consent line. Natively written, not translated — natural small-business register per Spec 22 tone rules; the "serious result, un-serious process" personality must survive the language switch. **A first native Icelandic draft now lives in the `is` catalog in `apps/web/src/lib/i18n.ts` (hero "Spunnin vefsíða. Alvöru vefur.", etc.); still needs founder QA/sign-off before launch.**
- [x] Build the typed locale-dictionary module at `apps/web/src/lib/i18n.ts`: `LOCALES = ['is', 'en']`, a `Strings` record type keyed by string IDs, `is` + `en` catalogs both satisfying it (TypeScript enforces completeness), and a `t(locale, key)` helper. No i18n framework for Phase 0 — at ~50 strings and 2 locales a hand-rolled module is the right size for a solo dev; migrate to a real framework only when builder-UI localization starts (hundreds of strings). **Done: `en` catalog is the typed source of truth, `is` satisfies `Strings`, plus a `translator(locale)` convenience binding. Catalog-parity unit tests in `i18n.test.ts`.**
- [x] Build the locale resolver at `apps/web/src/lib/locale.ts`: `?lang=` param → `localStorage` → `Accept-Language` → default `'is'` for the Iceland phase; constrained to the allow-list; `useLocale()` hook plus an SSR-safe resolver for head tags. **Done: pure `resolveLocale`/`localeFromAcceptLanguage` (quality-weight aware), `resolveRequestLocale` server fn for SSR head tags, `useLocale()` seeded from the SSR value (via loader data) and layered with `useSyncExternalStore` for the client `localStorage`/`navigator` signals — no hydration flash. Resolver unit tests in `locale.test.ts`.**
- [x] Replace hardcoded strings in `index.tsx` (~35, including the `spunFor` array and its hardcoded English conjunctions), `login.tsx` (~12), and `restore.tsx` (~4) with `t()` calls. **Done: all three surfaces render from the catalog; `madeFor` conjunctions are locale-keyed (`, `/` og `/`. ` for `is`). `ReturningWorkspacePrompt` takes a `locale` prop so it stays router-free for tests.**
- [x] Make `<html lang>` dynamic in `__root.tsx:103` and emit `og:locale` (`is_IS`/`en_US`) from the landing route `head()`. **Done: root `<html lang>` follows the visitor locale for the app/marketing surface (hosted public sites keep `en` until the published-site localization item lands); landing `head()` appends `og:locale`. Verified SSR + client via curl matrix and Playwright.**
- [ ] Defer legal pages (`terms.tsx`, `privacy.tsx`, ~110 strings): Icelandic legal text needs lawyer review, not a copy sprint; English is acceptable for MVP per Spec 22.

## P0 — Icelandic Site Generation (Specs 07 + 22)

`preferredLanguage` is accepted end-to-end but consumed nowhere: the UI never sends it, no system prompt contains a language instruction, and `internal/generation/service.go:2233` literally emits "Default locale is English." as a generated assumption. The Icelandic slug transliteration table already exists and is tested (`internal/platform/slugs/slugs.go` — þ→th, ð→d, æ→ae, "Snælda & Þing" → "snaelda-thing"); only the AI prompts need slug instructions.

- [x] Send `preferredLanguage` from the frontend: add it to the `streamGenerateSite` call at `app.index.tsx:253` (the API type already accepts it — this is the silent-drop bug), carry the landing-page locale into workspace start, and add `PreferredLanguage` to the interview request struct (`internal/generation/handler.go:81`). **Done: `SitesIndex` reads `useLocale()` and passes `preferredLanguage` to both `streamGenerateSite` and `fetchClarifyingQuestions`; the visitor locale is carried from the landing surface via the persisted `snaelda-locale` choice that `useLocale()` layers in. `interviewRequest` gained `PreferredLanguage` and forwards it into `BuildInterviewQuestions`. (True workspace-locale plumbing through the anonymous session start stays with the P1 `workspaces.locale` item.)**
- [x] Thread `PreferredLanguage` through every AI request struct and system prompt. Missing from: `PageLayoutRequest` (decomposed.go:63), `PageContentRequest` (decomposed.go:88 — **the primary copy stage**), `BlockSuggestRequest` (block_suggest.go:60), `CollectionDraftRequest` (openai.go:1037), `EntryDraftRequest` (openai.go:1052), `EntryRewriteRequest` (openai.go:1083), `ClarifyingQuestionsRequest` (interview.go:36), `PageChangeSetRequest` (page_change_set.go:42). Add a language clause to each system prompt with the Spec 22 Icelandic tone guidance; `changeSummary` strings are user-visible and must also conform. **Done: all eight structs (plus the collections-module request twins and the trimmed `pageChangeSetRequestPayload`) carry `PreferredLanguage`; a new `languageDirective()` helper (`internal/generation/language.go`) appends the Spec 22 Icelandic tone + slug contract to every copy-producing system prompt (legacy planner, outline, page layout, page content, clarifying questions, page change-set, block suggest, and the three collection/entry drafters). Verified by an HTTP-level integration test that the directive reaches the model's system message for `is` and is absent for `en`.**
- [x] Fix the locale drops in existing flows: `repromptSiteDecomposed` (decomposed_orchestrator.go:137) and `SuggestBlock` (block_suggest.go:137) both have `currentDraft.Site.DefaultLocale` available and don't forward it. **Done: both forward `currentDraft.Site.DefaultLocale`; the page-reprompt change-set + per-block rewrite (`page_reprompt.go`) and the whole-page reprompt (`buildWholePageRepromptPlan`) now thread the site locale too.**
- [x] Stop hardcoding `DefaultLocale: "en"` at site creation: `sites/mutator.go:1216` (`starterDraft`) and `mutator.go:89`; add `DefaultLocale` to `CreateSiteInput`; constrain `sites.default_locale` to `('is','en')` via migration check. **Done: `CreateSiteInput.DefaultLocale` + `resolveSiteLocale()` allow-list normalizer, `starterDraft` takes the locale, and migration `000026_site_locale_allowlist.sql` coerces legacy values then adds the `check (default_locale in ('is','en'))` constraint.**
- [x] Convert the deterministic fallback copy to locale-keyed string tables: the 14 English-only functions in `internal/generation/service.go` (`homeHeadline`, `homeSubheadline`, `aboutIntro`, `workProcessCopy`, `siteGoalForCategory`, `footerTagline`, `pricingPlansForProfile`, `testimonialItemsForProfile`, `faqItemsForProfile`, `teamPeopleForProfile`, the six page-plan builders) plus `starterDraft` copy and the `"Home"` nav label (mutator.go:1228). Remove the "Default locale is English." assumption (service.go:2233). Structure first, translations slot in after. **Done: the whole deterministic path is now data-driven — a locale→category `genCatalog` (`internal/generation/localized_copy.go` + `_en`/`_is` tables) backs `profilePrompt` (via `applyProfileCopy`), every page-plan builder (inline headings/CTAs/form labels/nav titles resolve through `genText`), the structured pricing/testimonial/faq/team lists, `siteGoalForCategory(category, locale)`, `assumptionsForProfile` (English-locale assumption deleted), and `repairGenerationPlan(plan, locale)`. `preferredLanguage` threads from `defaultGenerationPlanBuilder`/orchestrator/reprompt/imagery. Native Icelandic `is` catalog written in the Spec 22 register; `starterDraft` + Home nav localized via a sibling `internal/sites/starter_copy.go` catalog. Adding a locale is a data-only change (Spec 22 acceptance). Covered by catalog-parity + end-to-end `is`/`en` plan tests in `localized_copy_test.go` and `starter_copy_test.go`; `go test ./internal/generation/... ./internal/sites/...` green. The AI path still handles the primary case via `languageDirective`; this closes the fallback-path English leak.**
- [x] Add Icelandic slug instructions to the three AI prompts that generate slugs directly (`outlinePlannerSystemPrompt`, `collectionDrafterSystemPrompt`, `collectionEntryDrafterSystemPrompt` in openai.go) so "Þjónusta" becomes `/thjonusta`; extend `slugs_test.go` with the full Icelandic table. **Done: the slug transliteration table (þ→th, ð→d, æ→ae, ö→o, á/é/í/ó/ú/ý→…) rides in the `languageDirective` appended to those prompts; `slugs_test.go` gained `TestGenerateTransliteratesIcelandic` covering the full Spec 22 table.**
- [x] Add the language-conformance validation step (Spec 22: English must never leak into an Icelandic draft): hook between `repairGenerationPlan` and `buildDraftFromPlan` in `decomposed_orchestrator.go:97–103` and in the legacy retry loop (service.go:802–816), routing violations into the existing `feedback.ValidationIssues` retry mechanism. Lightweight detector; exempt proper nouns and user-provided verbatim text. **Done: `internal/generation/language_conformance.go` walks a plan's user-visible copy (siteGoal, page titles/SEO, and block-prop text — structural keys like href/slug/type/email/phone skipped) and flags any string carrying an English grammatical function word (`the`/`and`/`with`/`your`/… — a whole-word marker set that never occurs in Icelandic, verified against the `is` catalog, so proper nouns and loanwords pass clean). User-supplied text is exempted verbatim. Enforced for `is` only; `en`/unknown locales are a no-op. The legacy loop routes issues into `feedback.ValidationIssues` and retries, proceeding with a logged warning if leaks survive the final attempt; the decomposed pipeline surfaces a `siteconfig.ValidationError` so the caller falls back into that language-aware retry path. Covered by unit tests (clean Icelandic pass, multi-path English leak, verbatim + proper-noun exemptions, structural-prop skip, non-`is` no-op) and an end-to-end `Generate` retry test asserting the feedback thread. `go test ./internal/generation/...` green. Known follow-up: the deterministic repair fallbacks in `repair.go` (e.g. `"Learn more"`, `"Home"`) are content-word English the marker set deliberately does not flag; localizing those belongs with the repair-path locale work.
- [x] Localize published-site platform strings, keyed by the published snapshot's `DefaultLocale`: form success message (`internal/forms/service.go:35`) and validation error templates (service.go:327–453), the `og:locale` + `<html lang>` gap in published artifacts (`published-site.ts:26` sets neither), the English fallback description (`published-artifacts.tsx:213`). **Done: forms now resolve every visitor-facing string through a locale catalog (`internal/forms/messages.go`, is/en) keyed by `snapshot.Site.DefaultLocale` — the success confirmation and all validation messages (required/invalid-type/length/email/phone/option/unsupported/unknown) follow the site's content locale, not the builder UI. The published-site read path carries the locale end-to-end: `defaultLocale` rides the artifact manifest (`ArtifactManifest`), through the page cache (`PublishedPageArtifact`, `json:"-"`), and is promoted to `PublishedSiteResult.DefaultLocale` + the API JSON. The renderer writes it from `snapshot.site.defaultLocale` (`publishedSiteLocale`), and both English fallback descriptions (`Visit …`/`Discover more from …`) are locale-keyed. On the client, `buildPublishedPageHead` emits `og:locale` (`is_IS`/`en_US`) and `__root.tsx` drives `<html lang>` from the published locale (found in `/public/*` and custom-domain loader data) instead of the hardcoded `en`, with the visitor locale reserved for the app/marketing surface. Covered by Go tests (Icelandic success + validation messages; manifest→result locale plumbing with an `en` default for pre-locale manifests) and web tests (`og:locale` matrix, locale-reduction helpers, manifest `defaultLocale`, Icelandic fallback description). `go test ./internal/forms/... ./internal/publishing/...`, `npm run web:lint`, `npm run web:build`, and the affected vitest suites are green. Known follow-up: legacy artifacts published before this change carry no manifest locale and resolve to `en` until re-published (safe, unchanged behavior); the `<html lang>` for hosted published artifacts served as static files still comes from the app shell, which is the only rendering path in use today.
- [ ] Evaluate a stronger copy-stage model for `is` output (Spec 07 allows a per-stage model split); validate with real Icelandic prompts before Phase 1 reference builds.

## P1 — GTM Phase 0 Remainder: ISK Pricing, Icelandic Email, Footer Contract (Specs 15 + 18 + 04)

- [x] ISK catalog and per-currency price IDs (`internal/billing/catalog.go`): rename plan `basic` → `site` across catalog/service/access/tests, replace `MonthlyPriceUSD`/`OnceOverPriceUSD` with ISK-first per-currency fields (2900 / 6900 / 13900 — zero-decimal), extend `NewCatalog` to per-currency price IDs registered in `plansByPrice` (webhook plan derivation at service.go:659 depends on this map), rename env vars to `STRIPE_PRICE_SITE_ISK` / `STRIPE_PRICE_PRO_ISK` / `STRIPE_PRICE_ONCE_OVER_ISK` in `.env.example` + `internal/platform/config`, and make `CreateCheckoutSession` (service.go:148) select the price by workspace currency. **Done: `PlanDefinition` now carries a per-currency `Prices map[string]int` and unexported per-currency `priceIDs`; `NewCatalog(site, pro, onceOver map[string]string)` takes currency→price-id maps and registers every currency price id in `plansByPrice`. `planSite`/`"site"` replaces `planBasic`; `normalizePlan` coerces the legacy `"basic"` value to `site`, and migration `000028_rename_basic_plan_to_site.sql` rewrites persisted `plan='basic'` rows on `workspaces`/`billing_subscriptions`/`billing_entitlements`. `CreateCheckoutSession` resolves a workspace currency (a `workspaceCurrency` seam that returns ISK for the Iceland-only phase, where per-workspace currency plugs in with the P1 `workspaces.locale` work) and selects the price via `PriceIDForPlan(plan, currency)` / `OnceOverPriceID(currency)` with a default-currency fallback. Config/env/`server.go`/`handler.go`/`ServiceConfig` renamed to `STRIPE_PRICE_SITE_ISK`/`_PRO_ISK`/`_ONCE_OVER_ISK` + `SitePriceID`; `cmd/stripe-setup` provisions the ISK `site`/`pro`/`once_over` prices and prints the new vars. Adding a currency is data-only (register an amount + wire a price id). Covered by `catalog_test.go` (ISK prices, currency resolution with fallback, price→plan derivation, unconfigured-empty) and the updated checkout/webhook tests; `go test ./internal/billing/...` green.**
- [x] Fix receipt amount formatting for zero-decimal currencies: `formatAmount` (service.go:685–691) always divides by 100 — ISK 2900 would render "29.00". Emit `2.900` with Icelandic thousands separator; test ISK passthrough. **Done: `formatAmount` branches on a `zeroDecimalCurrencies` set (ISK, JPY, … per Stripe) — zero-decimal amounts skip the ÷100 and render through `groupThousands` with `.` grouping ("2.900"), while two-decimal currencies keep the `%.2f` path ("20.00"). Covered by ISK + USD tests in `catalog_test.go`.**
- [x] Currency-aware billing UI: remove the four hardcoded `$` sites in `app.billing.index.tsx` (lines 351, 398, 399, 577), update `BillingPlan`/`BillingCatalog` types in `api.ts` (547–563), render via `Intl.NumberFormat('is-IS', { currency: 'ISK', maximumFractionDigits: 0 })`, update `plan.id` union for `site`. **Done: `BillingPlan.prices` / `BillingCatalog.onceOverPrices` are per-currency maps and `plan.id`/checkout unions are `'site' | 'pro'`; the four `$` sites now render via a `formatPrice` helper using `Intl.NumberFormat('is-IS', { style: 'currency', currency, maximumFractionDigits: currency === 'ISK' ? 0 : 2 })` (ISK → "2.900 kr."). `humanPlan`/`planTagline`/`isRecommended` and the `app.tsx` + `app.sites.$siteId` plan-badge/checkout call sites all use `site`. `npm run web:lint`, `npm run web:build`, and the full 84-test vitest suite are green.**
- [x] `workspaces.locale` column + plumbing (migration 000026, `default 'is'`, checked to `('is','en')`): supply at both workspace-creation sites (`internal/auth/handler.go:682`, `:1205`), select in `lookupBillingContactTx` (billing/service.go:562), drive builder `<html lang>` and the email locale. **Done: migration `000029_workspace_locale.sql` adds `workspaces.locale text not null default 'is'` with a `check (locale in ('is','en'))` constraint. The trial-creation path threads the visitor's landing locale end-to-end — `SitesIndex`/`index.tsx` passes `useLocale()` into `startAnonymousSession({ locale })`, the `POST /api/sessions/anonymous` handler reads `?locale=`, and `createTrialSession(ctx, locale)` normalizes it (`normalizeWorkspaceLocale`, primary-subtag reduction, Icelandic default) and stores it on the workspace insert; the authenticated/legacy creation path relies on the column default (no request locale in scope there). `Session.WorkspaceLocale` is populated on every session-load path (`createTrialSession`, `loadTrialSession`, `resolveAuthenticatedSession` via `lookupWorkspaceLocale`) and surfaced to the client as `BuilderSession.workspaceLocale`. Billing now reads the locale: `lookupBillingContactTx` selects `w.locale` into `BillingContact.Locale`, and checkout derives its currency from it via `currencyForLocale` (Icelandic → ISK; other locales fall back to the default currency until a second market's prices are provisioned) — this replaces the placeholder `workspaceCurrency` seam from the ISK-pricing item. The builder `<html lang>` follows the workspace locale on `/app`: a small client store (`lib/workspace-locale.ts`, `useSyncExternalStore`) is published from the `/app` shell on session load and consumed in `__root.tsx`. Covered by Go tests (`normalizeWorkspaceLocale` matrix, `currencyForLocale`, billing contact-locale plumbing) and a web test asserting the landing locale reaches the session-start request; `go test ./internal/auth/... ./internal/billing/...`, `npm run web:lint`/`web:build`/`web:test` green. Note: the *email* locale is now available on `BillingContact.Locale` at the send sites, but consuming it (locale-keyed templates) is the next item's work (Spec 18); this item stops at making the locale available.**
- [x] Locale-keyed email templates (Spec 18): restructure `internal/email/templates/` into `is/`/`en/` subdirectories, update the `//go:embed` glob, add `locale` through `renderTemplate` → all `Render*` → all `Send*` helpers with `en` fallback, write the eight Icelandic template variants (subject + text + HTML), add per-locale parity tests. **Done: the sixteen existing template files moved under `templates/en/`, and eight native Icelandic pairs (subject + text + HTML) now live under `templates/is/`, written in the Spec 22 small-business register. The `//go:embed` glob is `templates/*/*.txt templates/*/*.html`; `renderTemplate(name, subject, locale, data)` resolves `<locale>/<name>.{txt,html}`. Subjects are locale-keyed via a `subjectFormats` table (fmt format strings, interpolating product/site name); `normalizeEmailLocale` reduces to the primary subtag and falls back to `en` per Spec 18. Every `Render*` and `Send*` helper gained a `locale` parameter, and all four call-site families forward the right source-of-truth locale: auth magic-link sends read `lookupWorkspaceLocale` (plus locale-aware `ActionLabel`/`ExpiresIn`), billing receipt/payment-failed/workspace-claimed and both once-over sends pass `contact.Locale`, and form-forwarding passes the published site's `form.Locale`. Billing amount formatting stays currency-driven (ISK no-decimal, done earlier). Covered by per-locale parity tests (every template renders non-empty subject/text/HTML with an `<html>` wrapper in every supported locale), an Icelandic magic-link render test, an English-fallback test for unsupported locales, a `normalizeEmailLocale` matrix, and updated real-caller billing webhook tests (English receipt via explicit `locale:"en"`, Icelandic once-over intake via the Iceland-first default). `go build ./...` and `go test ./internal/email/... ./internal/auth/... ./internal/billing/... ./internal/forms/...` green.**
- [x] Implement the structured Footer contact contract + the `showMadeWith` prop: migrate free-form address/hours to the structured shapes and emit real `PostalAddress` + `openingHoursSpecification` JSON-LD (`published-artifacts.tsx:398–411` previously stuffed the whole blob into `streetAddress`). **Done: the footer `contact` is now the Spec 04 structured shape — `address` is `{ street, city, postalCode, region, country }` and `hours` is a `{ day, opens, closes, closed? }[]` (canonical lowercase weekday keys, HH:MM clock times) — enforced by `validateFooterContact`/`validateFooterAddress`/`validateFooterHours` in `internal/siteconfig/blocks.go`, with the editor schema exposing a nested address object plus an hours repeater (day select + opens/closes + closed toggle) that the existing recursive `BlockEditor` renders. The new `showMadeWith` boolean (default true) drives a small "Made with Snælda" / "Gert með Snældu" link in the copyright band, toggleable by the owner. The generation repair path (`repairFooterContact`/`repairFooterAddress`/`repairFooterHours`) coerces model output into the structured shape (legacy single-line address folds into `street`; invalid days/times are dropped; weekdays deduped), and both deterministic footer plans carry `showMadeWith`. The renderer localizes weekday labels + the closed marker (is/en) via the site's `defaultLocale`, and `buildLocalBusinessJsonLd` now emits a real `PostalAddress` (streetAddress/addressLocality/postalCode/addressRegion/addressCountry) and `openingHoursSpecification` (schema.org dayOfWeek URLs, closed/partial days omitted). The `businessId` (kennitala) trust prop was cut — see Won't Do; Specs 04/22 and the GTM doc are updated to match. Covered by Go tests (structured validation accept/reject, legacy-string rejection, repair→validate round-trip) and web tests (structured footer + made-with rendering across is/en, opt-out, LocalBusiness JSON-LD emission). `go test ./...`, `npm run web:lint`, `npm run web:build`, and the 88-test vitest suite are green.**
- [x] Regenerate the block-registry golden fixture — `TestBlockRegistryContractFixtureMatchesGoldenFile` previously failed (`internal/siteconfig/contract_fixture_test.go:79`), and the footer prop additions changed the contract. **Done: `testdata/block_registry_contract.json` regenerated from `BuildBlockRegistryContractFixture()` (picking up both the previously-unsynced block taglines and the new footer contract); `go test ./...` passes again.**

## P2a — Re-spin Fidelity: Business-Integrity Fixes (TOP OF QUEUE, from 2026-07-12 QA)

Source: [respin-QA.md](respin-QA.md) — production QA of import `5f35b285` (mysewerguys.squarespace.com, plumber, Squarespace). Every item below was confirmed against source with file:line evidence. Goal: a re-spin must preserve the business's own facts — contact details, services, offers, testimonials, FAQs — so the owner recognizes their business instantly. Ordered by integrity impact.

- [x] **Harvest contact/NAP data before boilerplate stripping.** `internal/respin/extract.go:66-79` skips `<header>`/`<footer>`/`<nav>` subtrees and `findContentRoot` restricts prose to `<main>`; `resolveLink` drops `tel:`/`mailto:` hrefs. On most small-business sites (including the QA target) the phone, email, and street address live *only* there — so extraction's `Contact` came back empty and a 24/7 emergency plumber shipped with no way to call them. Add a full-document structured-signal pass (like `collectBrandHints`, extract.go:161, already does for logos): collect `tel:`/`mailto:` hrefs, address microdata/JSON-LD/og tags, and phone-shaped text from header/footer/announcement chrome, and feed them to `ExtractFields` as candidate facts. Boilerplate stripping should apply to prose, not to NAP signals. **Done: new `internal/respin/harvest.go` runs a whole-document `harvestContactSignals` pass (mirroring the `collectBrandHints` second walk) that the readability copy pass's `<header>`/`<footer>`/`<nav>` stripping no longer hides from — it collects `tel:`/`mailto:` hrefs (the highest-precision signals `resolveLink` discards), schema.org JSON-LD (`telephone`/`email`/`address` incl. nested `PostalAddress` assembled to one line), `itemprop` microdata (`telephone`/`email`), `<address>` elements, and — only when no higher-precision phone was found — phone-shaped text mined from the chrome (guarded by a 7–15 digit count so prices/dates/ids are rejected). Signals ride `Page.Contact` (`ContactSignals{Phones,Emails,Addresses}`, deduped case/space-insensitively), aggregate across every fetched page into `SourceContent.Contact`, and reach the model two ways: `promptDocument` renders a verbatim "CANDIDATE CONTACT SIGNALS" block and the extraction prompt tells the model to prefer them, *and* `ExtractFields` deterministically backfills any contact field the model still left empty (`backfillContact`, model's own value always wins, `MissingFields` recomputed). This directly fixes the QA failure where the phone/email lived only in a header/footer `tel:`/`mailto:`. Covered by unit tests (chrome tel:/mailto:/address, JSON-LD PostalAddress, microdata, phone-shaped fallback-only + digit guard, dedup, `extractPage` harvest-without-leaking-chrome-into-copy, `BuildSourceContent` aggregation + prompt rendering, backfill rescue/never-override, and the end-to-end `ExtractFields` path); `go test -race ./internal/respin/...` green.
- [x] **Thread the re-spin brief into the decomposed per-page calls.** `PageLayoutRequest`/`PageContentRequest` (`internal/generation/decomposed.go:63-98`) never received `input.Prompt` (see the call at decomposed_orchestrator.go:71) — only siteName, a one-sentence siteGoal, page goal, and contentBriefs. The brief *is* the re-spin contract (verbatim services/prices/hours/contact/testimonials from `composeBrief`, respin/compose.go:220), and the content composer's own prompt said "invent only if the prompt gives you enough" (openai.go:1551) while never seeing the prompt. Even a successful decomposed run degraded facts into placeholders. **Done: both request structs gained a `Prompt` field that rides the wholesale-JSON user payload (`json.Marshal(request)`), and `buildPagePlanFromLayout` gained a `prompt` parameter threaded from all three callers — the initial decomposed run passes `input.Prompt` (the composed brief), and both reprompt paths (`repromptSiteDecomposed` new-page branch + `buildWholePageRepromptPlan`) pass their page/site directive. The layout system prompt now instructs the model to give every fact in the brief a home on the page and carry it into `contentBrief`; the content composer prompt now treats the brief as authoritative and forbids overwriting a supplied fact (name/service/price/hours/phone/email/address/testimonial/FAQ) with a placeholder or invented substitute. Covered by `TestRespinBriefReachesPerPageCalls` asserting verbatim brief facts (phone + priced service) reach both the layout and content user payloads; `go test ./internal/generation/... ./internal/respin/...`, `go vet`, and `go build ./...` green.**
- [x] **Make the page_content schema structurally unable to drift, and retry per page.** The schema (`openai.go:389-401`) was `anyOf` over all layout block types at every index, then `validatePageContentMatchesLayout` (openai.go:451) hard-failed on positional mismatch — the model could legally return what the validator then rejected. **Done: `BuildPageContent` now emits `blocks` as an *object* with one required key per position (`block_0`…`block_N-1`), each pinned to exactly layout[i]'s type via a `const` plus a `$ref` to that type's prop schema hoisted into a root `$defs` (so a layout that repeats a block type stays compact — verified: a 3-block layout repeating `features_grid` produces 2 deduped defs and 3 pinned positions). Because position and type are now structural, not model choices, the model can no longer legally return a shape `validatePageContentMatchesLayout` then rejects — the drift is gone by construction. A new `pageContentPayload.toResult(layout)` flattens the position-keyed object back into the ordered `PageContentResult` the rest of the pipeline consumes; `validatePageContentMatchesLayout` stays as a defensive guard. Added **one per-page retry** in `buildPagePlanFromLayout` (called inside the orchestrator errgroup at decomposed_orchestrator.go:70): on a content-pass failure it re-runs the content call once with the failure threaded into a new `PageContentRequest.Feedback` field (`maxPageContentAttempts=2`, `pageContentRetryFeedback` helper), so one bad page is repaired in place instead of collapsing the whole decomposed run to the mega-call fallback. The layout system prompt now tells the model to **prefer `gallery` over `image_text`** for pages that show several work/product/portfolio photos (the `gallery`-vs-`image_text` failure mode) instead of fighting it, and the content prompt honors the `feedback` field. Covered by: updated `TestOpenAIPlannerBuildPageContentUsesSelectedLayoutSchemas` (asserts the position-keyed object, per-position `const` type + `$ref`, and `$defs` entries), the mismatch test (defensive validator still fires on a forced bad const), and new `TestBuildPagePlanFromLayoutRetriesWithFeedback` / `TestBuildPagePlanFromLayoutFailsAfterRetryExhausted` (retry recovers + threads feedback; exhausts at exactly 2 attempts). `go build ./...`, `go vet ./internal/generation/...`, and `go test ./...` green. (Pre-existing, unrelated `-race` data race in `TestRepromptSiteRewritesMatchingPagesInsteadOfCopyingBlocksVerbatim`'s shared fake planner is untouched by this change.)
- [x] **Fix brand pull wiring — it fails on 100% of imports, and the fix is a real design decision, not a one-liner.** `internal/respin/pipeline.go:156` calls `PullBrand` without `SiteID` and before any site exists; `brand.go:160-161` hard-errors without it. Result on every run: no logo, no source colors, no hero photos (the single strongest "that's my business" signal). **Done via design (a): pre-allocate the site id AND reserve the site row.** Design (b) was rejected because `validateAssetReferences` (sites/writer.go:548) already requires every referenced asset's `site_id` to be `""` or exactly `draft.Site.ID` — so the cleanest correctness contract is assets that are *site-scoped from birth*, which keeps `assets.ImportExternal`'s `SiteID`-required invariant intact (protecting the high-traffic normal-generation path) rather than relaxing it. The slug/creation-invariant "unless" the plan flagged is sidestepped: `generation.Service.ReserveSite` inserts a bare draft row (`draft_revision 0`, a UUID placeholder slug that the business-name slug generation never reproduces, is/en-normalized locale) so the brand pull can ingest the source's logo/hero photos against a real site, and generation's `SaveDraft` upsert then fills the reserved row in (revision 0→1, real slug overwrites the placeholder) — the reservation is invisible in the finished draft. On generation failure the pipeline calls `DeleteReservedSite` (guarded to `draft_revision = 0`, so a partially-saved site is never destroyed), leaving no orphan. Wiring: `GenerateInput.SiteID` (+ a dedicated `generationInputContext.PreallocatedSiteID`, kept separate from the reprompt/block-suggest `SiteID` so reprompt behaviour is untouched) threads the id into `buildDraftFromPlan`, which uses it instead of `ids.New()`; the pipeline reserves→`PullBrand(SiteID=reserved)`→`Compose(SiteID=reserved)`→generate, and `server.go` injects `genService` as the `Reserver`. The reserve/brand pull runs only when both a brand puller and a reserver are wired; otherwise generation mints its own id exactly as before. **Seed plumbing also closed:** `BrandResult.HeroAssetIDs` is no longer dead — `GenerateInput.SeedAssetIDs` carries the source's own pulled hero/work photos, `Compose` populates it from `brand.HeroAssetIDs`, and the imagery pass (`applyStarterImagery`/`fillBlockStarterImagery`) now prefers seeds over stock for hero/image_text/gallery slots via an ordered `seedPool`, falling back to Pexels only for slots the seeds don't cover (and seeding works even with no stock provider configured). Covered by unit tests (`buildDraftFromPlan` pre-allocated-vs-minted id; imagery seed-preference + seed-without-stock-provider; pipeline reserves-and-threads-brand-into-generation with ingest scoped to the reserved id + logo + seeds in the input; reserved-site cleanup on generation failure; `Compose` SiteID/SeedAssetIDs mapping) and a **real-Postgres integration test** (`reserve_integration_test.go`) that reserves a site, ingests a site-scoped brand asset, and asserts the `SaveDraft` upsert fills the reserved row (revision→1, placeholder slug overwritten) with the logo reference surviving `validateAssetReferences`, plus the revision-guarded delete no-op/cleanup. Fixed a pre-existing `-race` data race in the reprompt test's shared fake planner/suggester (concurrent slice appends) that this change surfaced. `go build ./...`, `go vet`, `go test ./...` (incl. `-race`), and `go test -tags integration ./internal/respin/... ./internal/generation/...` green. Follow-up (belongs to the queued GC item): unclaimed-import GC must cascade the pulled assets + reserved/generated site; `pulled_asset_ids` already records them.
- [x] **Widen the extraction schema — it is the ceiling on what can survive.** `ExtractedFields` (respin/extract_fields.go:13-25) carries name/tagline/about/services/hours/contact/testimonials and nothing else. The QA target's ~10 real FAQs, service-area list, client types, and current promo ("Spring Maintenance Special") had no field to land in and were discarded regardless of fetch/generation quality. Add `faqs`, `serviceAreas`, `clientTypes`, `announcements/offers` (all nullable, verbatim-fact rules), render them in `composeBrief`, and extend `resolvePages` hints. **Done: `ExtractedFields` gained `FAQs []ExtractFAQ{question,answer}`, `ServiceAreas []string`, `ClientTypes []string`, and `Offers []ExtractOffer{title,description}` — all nullable with the same "never invent" verbatim rules as the existing fields. The strict `extractionSchema` and `extractSystemPrompt` were extended (bounded array sizes; `missingFields` enum grown to all eleven fields), `normalizeExtraction` trims/drops-empties/dedups each new field, and `missingFieldsFor` flags them so the honesty signal stays accurate. `composeBrief` renders Offers, Service areas, Who-they-serve, and a real FAQ section (Q/A pairs), and `resolvePages` now adds a `faq` page when FAQs exist. Cross-language fidelity is preserved: `RewriteCopy` was extended to translate FAQ Q&A and offer title/description into the target language (index-pinned `minItems==maxItems` arrays, same as services), while service areas (place names) and client types stay verbatim; `hasRewritableProse` now also triggers on FAQ/offer prose so an FAQ-only source still gets a native-register rewrite. Covered by new compose tests (`TestComposeBriefRendersWidenedFields`, `TestResolvePagesIncludesFAQ`) and the existing normalize/rewrite suites updated for the new payload; `go test ./internal/respin/...` green.
- [x] **Brand color from external stylesheets (2026-07-13 QA, Spec 21 step 9 updated).** `collectBrandHints` (respin/brand_extract.go:53-61) reads only inline `<style>` blocks and `style=` attributes, and `parseCSSColor` (brand_color.go:101) parses only hex/`rgb()` — so every site-builder source (Squarespace/Wix/GoDaddy) misses its brand color: the palette lives in an external `site.css` as raw HSL triples referenced by button/accent variables. Fetch the page's first ~4 `<link rel="stylesheet">` targets through the existing SSRF-guarded fetcher (~500 KB cap each, inside the 40 MB import budget; failures non-fatal — fall down the existing chain); teach the parser `hsl()` and raw `H,S%,L%` triples (`*-hsl`-named vars); and score variables that back **primary-button backgrounds highest** (the CTA color *is* the brand color — verified on the QA target, where `--image-block-card-image-button-bg-color` resolves to `var(--accent-hsl)`), above the existing accent/brand/primary name-matching. This is the single highest-leverage fidelity fix: real brand color feeds the entire Spec 07/11 palette derivation. **Done: extraction now records external stylesheet targets (`PageMeta.StylesheetHrefs` via `readLink`, which stopped short at `icon` rels) and the raw inline CSS text (`PageMeta.StyleText`, in-memory only, 1 MB-capped, never persisted). `BrandPuller.PullBrand` runs a new `resolveCSSColors` pass first: it concatenates the inline CSS with a bounded fetch of the declared stylesheets (`fetchStylesheet`, `maxStylesheets=4` × `maxStylesheetBytes=500 KB`, deducted from the shared 40 MB import budget, cross-origin builder CDNs allowed but every hop still through the SSRF IP guard, HTML responses rejected, failures logged-and-skipped) and resolves colours over the *combined* text so a var() chain that spans a stylesheet boundary still resolves. `extractCSSColors` was rewritten around a custom-property declaration graph: it collects every `--x: value` (last-wins cascade), then `resolveCustomPropColor`/`resolveColorValue` follow `var(--y[, fallback])` references (cycle-guarded, depth-capped) carrying the referenced var's name forward as an interpretation hint — so `--…button-bg-color: var(--accent-hsl)` resolves through `--accent-hsl: 324.37,79.12%,51.18%` to the magenta `#e52095`. The parser learned `hsl()`/`hsla()` functions (`parseCSSColor`) and bare `H,S%,L%` triples gated on an `hsl`-declaring name (`parseCSSColorNamed`), with a real `hslToHex`. Scoring adds `button-bg`/`btn-bg`/`cta` keywords **above** primary/brand/accent, demotes hover/disabled/secondary/border roles, and caps near-greyscale/near-white/near-black resolved colours (`isVividHex`) so a white surface variable can't outrank the vivid brand accent. `ColorSource` stays `css-var` for both inline and external sources; the theme-color→logo-dominant fallback chain is unchanged. Covered by unit tests (var-chain+HSL resolution with white-not-winning, `hsl()`/`hsla()` parsing, name-gated bare-triple) and two httptest end-to-end `PullBrand` tests over the real SSRF fetcher (palette resolved from a fetched external stylesheet; stylesheet-404 falls back to theme-color); `go build ./...`, `go vet`, `go test ./...` and `go test -race ./internal/respin/...` green. Follow-up: computed-style button/`h1` sampling supersedes these heuristics once P2b's headless capture ships.
- [x] **Structured sourceHero extraction + hero photo scoring (2026-07-13 QA, Specs 21/07).** Two halves. *Extraction:* deterministically pull the source hero from the DOM (first section containing the `h1` / `section-background` patterns): headline, subheadline, CTA label, background image ref, `textOnly` flag; store it in `extracted_content`, carry it as `optionalHints.sourceHero` per the Spec 07 contract, and have the home-page `PageContentRequest` match its register. *Scoring:* `heroCandidates` gave `og:image` 55 vs content photos' 30 — the social card (often a logo lockup) beat the actual hero photograph; boost hero-region/section-background images above `og:image`. **Done.** *Extraction:* new `internal/respin/hero.go` reads the hero straight from the DOM (never an LLM) — `extractSourceHero` finds the hero root (first element named hero/banner/masthead/section-background, else the nearest `section`/`header`/`main` ancestor of the first `<h1>`) and pulls the headline (first `h1`→`h2`), a short supporting line (first `p`/`h2-h4` distinct from the headline, ≤240 chars), a CTA label (first non-chrome `a`/`button`, ≤40 chars, is/en chrome words excluded), and the background image (first `<img>` in the region, else a CSS `background-image:url()` — `data:` rejected), setting `textOnly` when no image. It rides `Page.Hero`, is persisted in `respin_imports.extracted_content` (pipeline `sourceHero` key), and reaches generation via a new first-class `generation.SourceHero` on `GenerateInput`→`generationInputContext`, attached **only to the home page** (`isHomeSlug`) in both `PageLayoutRequest` and `PageContentRequest` (they marshal whole, so the hint reaches the model). The layout prompt tells the model to make the first block a hero matching the source register + CTA intent (and prefer a text-led variant when `textOnly`); the content prompt draws the hero copy from the source headline's register/promise (rewritten naturally in the site language, never verbatim-cloned, CTA targeting a page that exists). The composer resolves `sourceHero.imageUrl` → the pulled asset id via a new `BrandResult.AssetIDByURL` and emits `generation.SourceHero`. *Scoring:* `extractPage` tags the hero background image's `ImageRef` with a new `"hero"` region (synthesizing one for a CSS-background hero); `regionRank` ranks `hero` above `header`, and `heroCandidates` scores a hero-region / `section-background`-hinted image at 90 — strictly above `og:image`'s 55, which is now explicitly the fallback. Covered by unit tests: hero extraction across text-only / `<img>` / CSS-background / h1-ancestor-fallback / no-heading, `cssBackgroundURL` parsing, hero-image region tagging, `heroCandidates` hero-region + section-background ordering above og:image, `composeSourceHero` asset resolution + text-only + omit-when-empty, `isHomeSlug`, `buildPagePlanFromLayout` attaches the hero to both calls, an OpenAI-payload test that `sourceHero` rides into both user payloads, and an end-to-end QA-scenario test (Squarespace-style page: real hero photo beats the og:image social card, punchy "Clogged Drain? We Fix Them 24/7" headline survives). `go build ./...`, `go vet`, `go test ./...` and `go test -race ./internal/respin/... ./internal/generation/...` green. Follow-up: the `textOnly`→`statement` variant mapping lands with the next item (the variant does not exist yet); the mega-call fallback path is unchanged (the decomposed path is primary for re-spin).
- [x] **Generate collections from the source's list-shaped content (services, people/authors) — TOP PRIORITY, added 2026-07-13.** No generation path emits collections today, even though the substrate is complete end-to-end: `SiteDraft.Collections` (with entries) persists via `replaceCollectionsAndEntries` (sites/writer.go:62), the site validator covers collections/pages/URL namespace (siteconfig/validate.go:57-59), and all three collection block types render. The only prompt→collection path is the interactive drafter on the collections page (collections/drafter.go); `generationPlan` (generation/service.go:883) has no collections field, re-spin's composer flattens extracted services into static brief text, and the layout catalog dead-locks the collection blocks ("Avoid: when no collection exists", layout_catalog.go:96-103) — so a re-spin of a six-service plumber or a twelve-person lab group ships flat `features_grid`/`team_profile_cards` blocks where a collection with index/detail pages is the obviously better artifact. Design is **deterministic-first**, matching the P2a verbatim-facts direction — extraction already produced the structured lists; turning them into a collection is a transform, not another model call:
  1. *Widen extraction* (respin/extract_fields.go) with `people []{name, role, bio}` — covers lab-group members, theatre casts/ensembles, and founders/team; services are already structured with verbatim prices. Same nullable never-invent rules, `missingFields` enum, normalization, and `RewriteCopy` handling (translate bios/roles into the target register, names verbatim). **The primary landing spot for people is the existing `team_profile_cards` block** (siteconfig/blocks.go:484 — a `{name, role, bio, photo, links}` repeater): render a People section in `composeBrief` so the brief-authority rule (shipped above) fills the repeater with the real names/roles/bios instead of the current "Team member / Role / Add a short bio…" default placeholder, and add a people hint to `resolvePages` (about/team page). This half is valuable on its own and ships even if the collection half slips.
  2. *Collection-worthiness decision in the composer* (respin/compose.go), per kind: services become a collection at ≥4 entries or when entries carry real per-item substance (description + price); people stay in the static `team_profile_cards` block by default and are only promoted to a collection for large rosters (≥6 — a theatre ensemble or lab group where per-person detail pages earn their keep; threshold is a soft call, tune in the 50-site QA loop). Below threshold, keep static-block behavior — a three-founder card row reads better than a near-empty collection.
  3. *Deterministic synthesis*: fixed per-kind `siteconfig.FieldDefinition` schemas mirroring what the interactive drafter would propose (services: title/description/price/image; people: name/role/bio/photo), entries verbatim from the (post-rewrite) extraction, slugs via `platform/slugs` (Icelandic transliteration free), validated with `siteconfig.ValidateCollection`. Entries must be `status=published` or the first publish trips the `no_published_entries` rule (validate.go:195-204).
  4. *New seam* `GenerateInput.SeedCollections []siteconfig.Collection` (same pattern as `SiteID`/`SeedAssetIDs`/`SourceHero`): `buildDraftFromPlan` attaches them to the draft and deterministically appends the index page (`type=collection_index` + `collection_index` block) and detail template (`type=collection_detail` + `collection_detail` block — it renders the entry generically, so no bindings needed for v1); nav sync via the existing page machinery.
  5. *Planner awareness*: pass the seeded collections (plural label + slug + entry count) into `PageLayoutRequest`/`PageContentRequest` context so `collection_list` becomes a legal, encouraged choice for home/landing pages (the catalog's "avoid" gate flips open), and slim the brief's corresponding section to a one-line pointer so the same facts aren't double-rendered into static blocks.
  6. *Follow-through*: the fact-coverage audit item below must count collection entries as valid landing spots for facts; seeded entries bypass the plan-level language-conformance walker (they arrive post-plan — `RewriteCopy` is the language guarantee, note the gap); entry image fields stay empty in v1 (extending the seed-imagery pass to entries is a follow-up); the unclaimed-import GC cascade already covers the site row, which owns the collections.
  This builds the exact seam the P5 "extend initial generation to structured content" item needs — re-spin is the first consumer because its input (a real site with real lists) makes the collection-worthiness call trivial. Spec 21 (pipeline) and Spec 07 (input contract) need a `seedCollections` note when this lands.
  **Done:** the deterministic backbone ships end-to-end. *(1) Widened extraction:* `ExtractedFields.People []ExtractPerson{name,role,bio}` (schema + `extractSystemPrompt` + `normalizeExtraction` + `missingFields` enum grown to twelve), and `RewriteCopy` translates each person's role/bio into the target register (index-pinned `minItems==maxItems` array like services) while the name stays verbatim (never sent to the model). `composeBrief` renders a real People section (name — role: bio) so the brief-authority rule fills the existing `team_profile_cards` repeater with real people instead of the "Team member / Role / Add a short bio…" placeholder, and `resolvePages` adds a `team` page hint when people exist and stay static. *(2) Worthiness in the composer* (`internal/respin/collections.go`, `synthesizeSeedCollections`): services become a collection at ≥4 entries or when every entry carries real per-item substance (description AND price) at ≥2; people are promoted only for large rosters (≥6) and otherwise stay in the static `team_profile_cards` block. *(3) Deterministic synthesis:* fixed per-kind `FieldDefinition` schemas (services: title/description/price/image; people: name/role/bio/photo), entries verbatim from the post-rewrite extraction, entry + collection slugs via `platform/slugs` (Icelandic transliteration free — `Þjónusta`→`/thjonusta`, entry slugs deduped), `status=published` (so the first publish never trips `no_published_entries`), `ExposeDetailURLs=true`, each collection validated with `siteconfig.ValidateCollection` and silently dropped (that kind stays static) if it fails — so this can never feed generation an invalid draft. *(4) New seam* `GenerateInput.SeedCollections []siteconfig.Collection` (mirrors `SiteID`/`SeedAssetIDs`/`SourceHero`, threaded through `generationInputContext` and both live callers of `buildDraftFromPlan`; reprompt passes nil): `buildDraftFromPlan` attaches the collections and appends, per collection, a deterministic `collection_index` page (public listing at `/{slug}`, added to primary nav, `collection_index` block) and a `collection_detail` template page (per-entry rendering, **not** in nav, `collection_detail` block), with a `uniquePageSlug` guard so the synthesized slugs never collide with a planner page. *(5, partial) Planner awareness:* the composer slims the brief's promoted section to a one-line pointer ("the site already has a Services collection with N entries… do not re-list as static cards") and drops the corresponding static-page hint from `resolvePages`, so the same facts are not double-rendered and the collection index owns `/{slug}`. *(6) Follow-through:* seeded entries bypass the plan-level language-conformance walker by construction (they are attached in `buildDraftFromPlan`, after the plan walk — `RewriteCopy` is the language guarantee, documented on the seam), and entry image fields stay empty in v1 (asset fields are optional). Covered by Go tests — `collections_test.go` (worthiness matrix, services/people synthesis incl. Icelandic transliterated slug, entry-slug dedup, `ValidateCollection` round-trip, no-promote for small lists), `compose_test.go` (Compose wires `SeedCollections`, slims the brief, drops the `services` page hint; small team stays static with real people in the brief), `stages_test.go` (people role/bio rewritten, name verbatim), and `generation/seed_collections_test.go` (index/detail pages + nav wiring, slug-collision suffixing, and the full **publish-gate** check — the draft maps to a `PublishedSnapshot` that passes `ValidatePublishedSnapshot`, exercising `no_published_entries` and entry-URL collisions). `go build ./...`, `go vet`, `go test ./...`, and `go test -race ./internal/respin/... ./internal/generation/...` green. **Follow-up (deferred, safe):** the second half of step 5 — threading the seeded collections into `PageLayoutRequest`/`PageContentRequest` and flipping the `layout_catalog` "avoid when no collection exists" gate so the planner can *feature* the collection with a `collection_list` block on the home page — is not wired yet, because the LLM would need the collection's id/slug in-context to fill the `collection_list.collection` prop and an empty/wrong value would fail validation and collapse the run. The deterministic `collection_index` page + nav entry already surfaces every collection at a public URL, so this is a pure enhancement; wire it alongside the P5 prompt-path collection-worthiness item, which needs the same layout-request plumbing.
- [x] **`statement` hero variant — type-led, image-free (Spec 04 updated, `hero@1.1.0` additive).** The hero block's image is already optional, but a text-only `standard` hero renders modest; sources like the QA target work because they're oversized display type on the brand color. Add the `statement` variant (oversized headline on a solid brand-primary background, `image`/`layout` ignored, passthrough migration) to `heroBlockDefinition` + `validateHeroProps` (siteconfig/blocks.go), the preview/published renderers (design work — BRANDING.md/DESIGN.md dark-mode direction applies), and planner guidance: pick it for text-led trades with no hero photography and for re-spins with `sourceHero.textOnly`. Depends on the sourceHero item for the re-spin trigger; ship the variant itself independently. **Done (shipped in `4ca2d50`): `hero@1.1.0` registers the `statement` variant with 1.0.0 kept alive (shared schema/validator — additive) so stored drafts keep resolving; `validateHeroProps` accepts `statement` in the variant enum and `image`/`layout` stay optional/ignored by contract. Both preview and published sites render through the same `SiteDraftRenderer` (published-artifacts.tsx composes it), which paints the poster composition — edge-to-edge brand-primary fill, oversized display headline on a `statement` type-scale step in the theme background color, inverted CTAs. Planner guidance in `layout_catalog.go`/`openai.go`/`page_reprompt.go`/`repair.go`/`service.go` steers text-led trades and `sourceHero.textOnly` re-spins to it. The commit also fixed the renderer-wide `text-[var(--size-*)]` → font-size bug the variant surfaced. Verified complete against current source this session (checkbox was stale).**
- [x] **Post-generation fact-coverage audit — know integrity was kept, don't hope.** The QA run reported `succeeded, degraded=false` while shipping zero contact info. After generation, reconcile the draft against `ExtractedFields` with plain string containment (no LLM call): every service name, the phone, the email present somewhere in the draft. On misses, run a targeted repair (inject into footer/contact/features blocks deterministically) or mark the import degraded with the missing facts listed. This converts silent content loss into a visible, testable contract — and gives the 50-site QA loop a scorable metric. **Done: `internal/respin/coverage.go` adds a pure, LLM-free `AuditFactCoverage(draft, fields) FactCoverageReport` that walks every user-visible string in the generated draft — page-block props (recursively), footer contact, brand/site name + SEO, nav labels, and collection entries (services promoted to a collection live in entries, not static blocks — counted as valid landing spots per the P2a collections item's follow-through) — and reconciles the highest-integrity facts against `ExtractedFields`: the phone, the email, and every named service. Matching is formatting-tolerant: services/email by whitespace-collapsed case-insensitive containment; phone by trailing-7-significant-digit match per text node, so `+354 555 1234` in extraction and `555-1234` in the draft (or vice-versa) reconcile without a false miss, and a too-short number is treated as covered rather than force a false degrade. The pipeline (`pipeline.go`) runs the audit after a successful generation and, on any miss, marks the import `degraded` with a compact reason (`missing_facts: phone, email, service "Drain Cleaning"`) via `MarkDegraded`, combining with any compose-time reason (`joinDegradeReasons`) — converting the false `succeeded, degraded=false` into an honest, scorable signal while keeping the generated draft (soft degrade: the after-view still renders, `SiteID` preserved). `RunResult.Degraded` now reflects the audit. Covered by unit tests (`coverage_test.go`: all-present pass, missing phone/email/one-of-two-services with reason detail, phone country-code/formatting tolerance + different-number miss, facts satisfied by collection entries, empty-extraction no-op) and a pipeline regression (`TestPipelineDegradesOnMissingFacts`: a generation that drops facts degrades with phone/email/service listed, draft kept). `go build ./...`, `go vet`, `go test ./...`, and `go test -race ./internal/respin/...` green. Follow-up (deferred, safe): the "or run a targeted repair (inject into footer/contact/features)" alternative — deterministically re-homing a missing fact and re-saving the draft — is a pure enhancement on top of this honest-degrade contract; the audit + visible signal is the scorable metric the 50-site QA loop needs.
- [x] **Prompt parity + placeholder hygiene on the fallback path.** The mega-call `generationPlannerSystemPrompt` (openai.go:1388) lacked the decomposed layout prompt's contact_form rule (openai.go:1533) — the QA draft used `testimonials`/`faq` blocks headed "Get in touch" as fake contact sections, with hero CTAs pointing at a nonexistent `/#contact`. Port the rule; broaden `generatedTextMentionsContactForm` (repair.go:355) beyond "contact form"/"inquiry form" (e.g. "use this form", "request service"). And stop `repairTestimonialsProps` (repair.go:580-586) injecting "Add one concise testimonial… — Client name" into fresh generations: on first-generation/re-spin drafts, drop empty repeater blocks instead of stuffing them with meta-instructions (keep the placeholder behavior for in-builder edits if desired). **Done: the mega-call `generationPlannerSystemPrompt` gained a "Block rules" clause porting the decomposed layout prompt's contact_form guard (never use testimonials/cta_band/text_section as a stand-in form, never head a non-form block "Contact us"/"Get in touch" as one) plus a CTA-destination rule (link only to a page that exists in the plan or an on-page anchor a block actually renders — no invented `/#contact` hrefs). `generatedTextMentionsContactForm` now scans a `contactFormIntentMarkers` list broadened with explicit form-action phrasing ("use this form", "fill out the form", "request service", "request a quote", "send us a message", plus Icelandic "eyðublað"/"fylltu út") while deliberately excluding generic nav CTAs ("get in touch"/"contact us") so a cta_band that merely links to a contact page isn't rewritten into an inline form. Placeholder hygiene: all six repeater repairs (`repairTestimonialsProps`/`repairFAQProps`/`repairFeaturesGridProps`/`repairGalleryProps`/`repairPricingPackagesProps`/`repairTeamProfileCardsProps`) now return a `keep bool`, and a `dropEmptyRepeaters` flag threads `repairGenerationPlan`→`repairPages`→`repairHomePage`/`repairSecondaryPage`→`repairBlocks`→`repairBlockPlan`: on a fresh first-generation/re-spin draft (both decomposed-orchestrator initial + mega-call `generateDraftWithRetry`, `true`) an empty repeater is dropped entirely rather than shipping a meta-instruction placeholder the visitor would see; on in-builder reprompt (whole-page + per-page fallback, `false`) the placeholder is kept so a mid-edit section stays visible. `repairBlocks`' all-blocks-dropped text_section fallback still guarantees a non-empty page. Covered by new unit tests (drop-on-fresh vs keep-placeholder-on-reprompt for testimonials+faq; `generatedTextMentionsContactForm` positive form-intent + negative generic-CTA matrix) and the existing intended-contact-form-conversion tests still green. `go build ./...`, `go vet ./...`, and `go test ./...` green.
- [x] **Discovery scoring: stop spending the page budget on /cart and /privacy-policy.** `highValuePaths` (respin/discover.go:134) lacks `faq`; with `defaultMaxPages = 5` the QA target's `/faqs` (10 real Q&As) lost its slot to zero-score chrome links. Add `faq`/`faqs`/`spurningar`, and explicitly down-score `cart|privacy|terms|login|cookie`. **Done: `highValuePaths` gained `faq`/`faqs`/`spurningar`/`algengar-spurningar` (ranked just below about/services/contact), and a new `lowValuePaths` list negatively scores chrome/transactional slugs (`cart`/`checkout`/`karfa`, `privacy`/`persónuvernd`/`cookie`/`vafrakokur`, `terms`/`skilmálar`, `login`/`innskráning`/`account`/`reikningur` — English + Icelandic). `pageScore` checks the low-value list *first* so a slug like `/terms-of-service` (which contains the high-value "service" substring) is correctly demoted, and negatives sort below zero-scored generic content so junk never steals a slot within the small page budget. Covered by `TestPageScoreRanking` (faq positive; cart/privacy/terms/login/cookie negative and below both a generic page and the faq page) and `TestFetchSiteFAQBeatsChrome` (over a real httptest server, `/faqs` wins the single budget slot over `/cart` + `/privacy-policy`).
- [ ] **Per-stage model evaluation (after the structural fixes above).** Everything runs on `gpt-5-mini` (`internal/platform/config/config.go:125`). Evaluate a stronger model *per stage* (Spec 07 allows the split; merge with the open P0 `is`-copy-model item): strongest candidates are `page_content` (copy quality/specificity) and the fallback `site_generation_plan`; keep mini for classify/extract/rewrite unless extraction-accuracy testing says otherwise. Measure on re-spins with the coverage audit as the metric, not vibes — the schema-pinning fix above removes the type-drift failure deterministically, so don't buy a bigger model to fix what a schema fixes for free. Re-spin budget math: public demo has a 3M-token/day ceiling (`RESPIN_DAILY_LLM_TOKEN_BUDGET`); a ~30k-token run at a ~5× stronger-model price is still cheap per demo wow.
- [ ] **Copy/polish pass on the generated draft:** dedupe the tagline (QA draft reused "Quick, 24/7 service" as home subheadline, about subheadline, and footer tagline), keep business-name usage consistent (site titled "The Sewer Guys, LLC", about body "My Sewer Guys is…"), ensure hero CTAs target anchors/pages that exist, and prefer the source's punchier voice ("Clogged Drain? We Fix Them 24/7") over flattened summaries where the brief carries it.
- [ ] **QA observability:** expose the stored extraction + composed brief (`respin_imports.extracted_content`, SaveExtraction at pipeline.go:181) in a debug/admin view or flag-gated share field, so QA can distinguish "extraction missed it" from "generation dropped it" without production DB access.

## P2 — Re-spin v1 + Public Before/After Demo (Spec 21)

No re-spin code exists (verified: zero hits). The substrate is strong: `assets.Service.ImportExternal` (assets/service.go:275) already does server-side ingest with `SourceURL` provenance; the SSE streaming loop (generation/handler.go:706), `GenerateInput` contract (service.go:48), anonymous-session/claim machinery (auth/handler.go:110, :885), preview tokens, and three durable rate-limiter patterns are all reusable. Build order:

- [x] `respin_imports` entity: migration (table + `normalized_url` index + nullable `generation_jobs.respin_import_id`), store layer in `internal/respin/`. Unclaimed demo imports create a real trial workspace up front (the existing `createTrialSession` path) so the `assets.workspace_id` constraint holds. **Done: migration `000030_respin_imports.sql` creates the Spec 21 table verbatim — nullable `workspace_id` (FK `on delete cascade`) + `guest_session_id` (`on delete set null`) so a public-demo import can predate any workspace, `fetch_status`/`fetch_mode` CHECK enums, `extracted_content`/`classification`/`error` jsonb, `pulled_asset_ids` jsonb default `'[]'`, `degraded`/`degradation_reason`, unique `share_slug`, the `normalized_url` index, and the `set_updated_at` trigger; it also adds the nullable `generation_jobs.respin_import_id` FK (`on delete set null`) + partial index, and `PromptActionInput.RespinImportID` threads it through the job insert in `prompt_governance.go`. The store (`internal/respin/service.go`, matching the `forms` pgx `DB`-interface/`Service` convention) covers the full lifecycle: `Create` (queued, unclaimed), `Get`/`GetByShareSlug`, `FindCached` (most-recent succeeded/degraded within a TTL window — backs the per-normalized-URL result cache), `UpdateStatus` (state machine + fetch-mode recording that never clears a set mode), `SaveExtraction`, `SavePulledAssets`, `AssignShareSlug`, `MarkDegraded`, `Fail`, `Claim` (binds workspace on sign-up, rejects re-claim with `ErrAlreadyClaimed`), and `DeleteUnclaimedBefore` (GC). Verified end-to-end against a real Postgres: goose up/down/up round-trips cleanly, `\d respin_imports` matches the spec (checks, FKs, indexes, trigger, `generation_jobs` back-reference), and two `//go:build integration` round-trip tests (`service_integration_test.go`) exercise the whole lifecycle incl. the job→import provenance linkage — `go test -tags integration ./internal/respin/...` green, full `go test ./...` green. Note: `workspace_id` stays nullable per Spec 21 (the demo import exists before the workspace); the "create a trial workspace up front" runtime wiring lands with the `POST /api/respin` endpoint + claim item below, which the store's `Claim`/nullable-workspace paths already support.
- [x] SSRF-guarded fetcher: custom dialer that resolves DNS, rejects private/loopback/link-local/metadata ranges, pins the vetted IP, re-checks every redirect hop (cap 5); 10s timeout, 5 MB cap; readability-style extraction; content-sufficiency check; same-origin discovery capped at ~5 pages. Headless fallback is deferred to v1.1 — degrade to the prompt flow when plain fetch is insufficient. **Done: `internal/respin/fetch.go` builds an `http.Client` whose `net.Dialer.Control` hook validates the *resolved* `ip:port` right before connect (`guardDialAddress` → `isBlockedIP`, `ipguard.go`) — so DNS rebinding cannot flip an internal address past a hostname check, because the address validated is the exact one the socket dials. The guard rejects non-`http`/`https` schemes, any port but 80/443, credentials-in-URL, and every non-routable range: loopback, RFC 1918 + IPv6 ULA, link-local (incl. the `169.254.169.254` cloud-metadata endpoint), CGN `100.64/10`, IETF/TEST-NET/benchmark/reserved blocks, the NAT64 well-known prefix, and IPv4-mapped-IPv6 forms of all of the above. `CheckRedirect` re-validates the URL invariants and caps at 5 hops; bodies are read through an `io.LimitReader` at a 5 MB HTML cap (10 MB image cap available via `FetchOptions` for the later asset-ingest item). `extract.go` does readability-style extraction with `golang.org/x/net/html`: prefers `<main>`/`<article>`, strips script/style/nav/header/footer/aside/form/hidden boilerplate, and captures title, meta description, and the head-level brand hints (og:image resolved absolute, theme-color, icon links, lang) the brand-pull stage will reuse. `Page.Sufficient()` gates on a 25-word floor tuned to separate a near-empty client-rendered shell from a genuinely thin real homepage → `ErrInsufficientContent` drives the degrade-to-prompt path. `discover.go`'s `FetchSite` follows up to 5 same-origin pages, ordered by a high-value-slug heuristic (about/þjónusta/hafa-samband/verð…), skipping asset extensions and honouring `robots.txt` (`robots.go`, wildcard + explicit `SnaeldaRespin` group, permissive on failure). `normalize.go`'s `NormalizeURL` is the intake/cache-key canonicalizer (scheme+host lowercase, fragment/tracking-param strip, sorted query, trailing-slash trim) feeding the store's `FindCached`. Honest `SnaeldaRespin/1.0` UA. Added `golang.org/x/net` as a direct dependency (`go mod tidy` also bumped x/text/x/sync minor versions). Covered by unit + httptest integration tests: IP-range matrix, dial guard, scheme/port/credentials/redirect-cap/size-cap fetch tests, a production-guard-blocks-loopback test, readability extraction + boilerplate/hidden-text exclusion, sufficiency, multi-page discovery with robots + page-budget + high-value ordering, URL-normalization/cache-key parity, and robots parsing; `go test ./internal/respin/...` (incl. `-race`) green, full `go test ./...` green. A live check confirmed the production fetcher blocks `localhost`/`127.0.0.1`/`[::1]`/`169.254.169.254` while fetching and extracting `example.com`. This item builds the fetch/extraction substrate; the LLM classify/extract/rewrite stages, brand/asset pull, composer, and public endpoints are the separate downstream P2 items that consume `FetchSite`.
- [x] LLM stages: classify (vertical/locale/tone), structured field extraction (name, services, hours, contact, about, testimonials — nullable), copy rewrite into the target language per Spec 22. Daily LLM cost budget for the unauthenticated tier with a friendly busy response. **Done: the three Spec 21 LLM stages live in `internal/respin` behind a small `Completer` seam (`llm.go`, an OpenAI-compatible strict-`json_schema` client that reports token usage and returns `(nil, nil)` with no key so the pipeline degrades). An `Analyzer` (`analysis.go`) runs them over a `SourceContent` built from the fetched pages (`BuildSourceContent`, bounded to 24k chars): `Classify` → `Classification{vertical, services, locale, tone, confidence}` with a confidence floor that flags low-confidence runs for the generic-block-set degrade path; `ExtractFields` → a fully-nullable `ExtractedFields` (business name, tagline, about, services w/ verbatim prices, opening hours canonicalized to lowercase-English weekday + `HH:MM` matching the footer contract, contact, verbatim testimonials) whose `missingFields` flags are recomputed from the cleaned result, not the model's self-report — nothing is fabricated; `RewriteCopy` rewrites only the prose (tagline/about/service copy) into the target language in the Spec 22 Icelandic register while preserving every verbatim fact (name, prices, hours, contact, testimonials), and skips the call entirely when there is no prose. `Analyze` chains all three, resolves the target locale (override → detected → Icelandic default), and returns a `Degraded` flag. The **daily LLM cost budget** is durable: migration `000031_respin_llm_usage.sql` + `PostgresBudgetStore` (atomic per-UTC-day upsert) back a `Budget` that every stage checks before spending and records after; exhaustion returns `ErrBudgetExhausted` (the friendly-busy signal the public endpoint will map), a nil budget is the always-allow session-bound path, and `RESPIN_DAILY_LLM_TOKEN_BUDGET` (default 3M, 0 disables) configures the ceiling. Covered by unit tests (classification normalization/low-confidence, extraction cleaning + weekday/clock canonicalization + missing-field recompute, rewrite merge/verbatim-preservation/skip, `Analyze` chaining + locale override + degrade, budget enforce/record/reset/nil) and a Postgres integration test for the atomic upsert; `go test ./internal/respin/...` (incl. `-race` and `-tags integration`) and `go build ./...` green. This closes the classify/extract/rewrite stages; wiring the `Analyzer` + budget into the public `POST /api/respin` endpoint (and the friendly-busy HTTP response) is the downstream endpoints item.**
- [x] Brand/asset pull: logo heuristics (icon links, og:image, header img scoring), primary color (CSS custom props, theme-color meta, logo dominant color), hero photos — all through the SSRF client into `ImportExternal`; asset ownership transfers on claim, unclaimed assets GC'd with the import cache. **Done: the single readability parse now also captures the brand hints it used to discard — every `<img>` tagged with its structural region (`header`/`nav`/`footer`/`content`), class/id/alt/size hints, richer `<link rel=icon>` refs (rel + sizes), and CSS custom-property colours from `<style>` blocks and inline `style=` — added to `PageMeta` via a second whole-document walk (`brand_extract.go`) that deliberately visits the `<header>` chrome the copy pass skips (where logos live). `internal/respin/brand.go` implements `BrandPuller`: it ranks logo candidates (named header/nav `<img>` > plain header `<img>` > apple-touch-icon > sized rel-icons > og:image), fetches each best-first through the existing SSRF-guarded `Fetcher` (10 MB image cap, per-import 40 MB byte budget, content-type allow-list + `http.DetectContentType` sniff fallback), and ingests the first success through an `AssetIngestor` seam that `*assets.Service.ImportExternal` satisfies directly — with `source:'respin'` + import id + origin URL provenance and decoded width/height. Primary colour resolves in Spec 21 priority order — CSS custom property (`--primary`/`--brand`/`--accent`… scored) → `theme-color` meta → dominant colour of the decoded logo (`brand_color.go`: HSV-filtered vivid-pixel histogram that ignores transparent/greyscale/near-black pixels; png/jpeg/gif decode, webp/avif ingest without a decoded colour). Hero photos are large content/og images (logo-like and sub-200px images excluded, verified by decoded dimensions, capped at `maxHeroes`, deduped against the pulled logo). Output is a `siteconfig.BrandConfig` (BusinessName + `Logo{AssetID,Alt}` + PrimaryColor) plus `HeroAssetIDs` and the full `PulledAssetIDs` list for `respin_imports.pulled_asset_ids` and unclaimed-asset GC — dropping straight into Spec 07's first-class `brand` input. Never dead-ends: a nil ingestor/fetcher or all-failed fetches yield a thinner brand (business name preserved, colour still resolved from CSS). Covered by 12 tests over an httptest server + real SSRF fetcher + fake ingestor (end-to-end happy path with provenance/dims/hero-count assertions, degrade-without-ingestor, ownership guard, images-404 colour fallback, theme-color and logo-dominant colour paths, byte-budget, CSS colour ranking/parse, greyscale-ignoring dominant colour, content-type sniff, logo-candidate ordering); `go test -race ./internal/respin/...`, `go vet`, and `go build ./...` green. The runtime wiring (creating the trial workspace/site up front, calling `PullBrand` from the pipeline, transferring ingested assets on claim, GC of unclaimed assets) lands with the downstream composer + public-endpoints + claim items, which consume this stage.
- [x] Composer: extracted fields + brand + classification → exactly `generation.GenerateInput` (the Spec 07 contract), then `GenerateWithProgress`; degraded compose path records `degradation_reason` and pre-fills the prompt flow — never a dead end. **Done: `internal/respin/compose.go` turns an `AnalysisResult` + `BrandResult` into the canonical Spec 07 `generation.GenerateInput` (Spec 21 "Hard Contract") — no second draft format. `Compose` synthesizes the business brief that satisfies the generation job's not-null `prompt` column: a verbatim-facts instruction header followed by only the sections the extraction populated (name/industry/tone/tagline/about, services with their verbatim descriptions+prices, opening hours in calendar order with a Closed marker, contact lines, verbatim testimonials, source URL). Brand assets (logo asset id, primary colour) are carried **verbatim** into the first-class `brand` input — generation reuses, never reinvents; the business name is backfilled onto the brand config when the pull missed it. `OptionalHints` carries the classified `industry` and a `pages` set derived from what content actually exists (home always, then services/about/contact). `PreferredLanguage` resolves target→classification→Icelandic default. Degradation is structural, not bolted on: a soft degrade (low classification confidence) still yields a full input with `Degraded`/`DegradationReason` threaded to the import record; `ComposeDegraded(reason, salvage, brand, ctx)` handles the hard-degrade path (thin content / fetch failure / budget) by building a thin instance of the same contract whose prompt is a natural-language `PromptPrefill` ("A website for <name>, a <vertical> business. <tagline/about first sentence>.") the demo UI drops into the ordinary homepage prompt flow — never a dead end. Covered by unit tests (canonical-input shape + verbatim brand, section omission for empty content, soft-degradation passthrough, natural-reading prefill, Icelandic locale default, salvage-based degrade incl. empty salvage, page resolution, calendar-order hours); `go test -race ./internal/respin/...`, `go vet`, and `go build ./...` green. The runtime wiring — calling `Compose`/`ComposeDegraded` from the pipeline, then `GenerateWithProgress`, and writing the `respin_imports` state — lands with the downstream public-endpoints item, which the composer's pure-transform API is shaped for.**
- [x] Public endpoints + guards: `POST /api/respin` (normalize, cache by URL, SSRF pre-check, per-IP limit via the auth IP-limiter pattern), SSE progress, preview (demo-scoped preview token), claim (binds workspace, books one prompt via `PromptActionManager`), share; session-bound `POST /api/sites/respin` for existing workspaces; global concurrency semaphore. **Done: the runtime wiring the earlier substrate items deferred to "the public-endpoints item" now lands. A `Pipeline` (`internal/respin/pipeline.go`) orchestrates fetch→analyze→brand→compose→`GenerateWithProgress`, advancing the import state machine and degrading structurally at every tier — hard degrade (thin content / fetch failure / budget) records a salvaged `promptPrefill` and never generates unattended; soft degrade (low confidence) still generates and flags `degraded`; a generation error moves the import to `failed`. A `Runner` (`runner.go`) owns the **global concurrency semaphore** (`RESPIN_MAX_CONCURRENT_IMPORTS`, default 3; over-capacity → friendly busy) and an in-process progress hub that replays buffered events so an SSE watcher connecting after the start POST still sees the whole stream. The `Handler` (`handler.go`) serves `POST /api/respin` (normalize → per-IP durable limit via `auth.IPRateLimiter` at 3/h+10/day → SSRF pre-check via the new `Fetcher.ValidatePublicURL` → daily-budget gate → per-normalized-URL cache via `FindCached`/`RESPIN_CACHE_TTL` → slot reserve → detached demo trial workspace → import → background run), `GET /api/respin/imports/{importId}` (JSON status or SSE progress; the import-scoped routes sit under a literal `/imports/` segment so the `{importId}` wildcard cannot collide with `/share/{shareSlug}` under net/http pattern precedence), `GET …/preview` (source URL + demo-scoped preview token from `sites.PreviewTokenService.Issue`, or the salvaged prefill when degraded), `POST …/claim` (adopts the up-front demo session's cookies via the new `auth.AdoptRespinDemoSession`, binds `respin_imports.workspace_id` via `store.Claim`; the demo generation already booked the one prompt through the workspace quota, so no double-charge), `GET /api/respin/share/{shareSlug}`, and the session-bound `POST /api/sites/respin` (behind `requireUser`, enforces site+prompt limits, unbudgeted analyzer). The unclaimed-demo model matches the store as built: the import stays `workspace_id NULL` while a detached trial workspace (new `auth.StartRespinDemoSession`) holds the generated site + assets, tracked via the `generation_jobs.respin_import_id` back-reference (new `store.LinkGenerationJob`/`LinkedGeneration`) which also feeds the publish gate. The public demo POST/claim routes are CSRF-exempt (they establish their own cookies and carry an unguessable import-id capability); the session-bound route keeps CSRF. Two pipelines share one runner — the public analyzer carries the daily LLM budget, the session-bound one does not, keeping the quota-accounted path off the public ceiling (Spec 21). `generation.NewServiceFromConfig` was extracted so re-spin and the generation handler build the same service. Covered by unit tests (runner semaphore + hub replay + concurrent subscribers under `-race`; pipeline generate/thin-degrade/analyzer-unavailable-degrade/generation-failure with fake store+generator over a real SSRF fetcher; `ValidatePublicURL` scheme/creds/loopback/metadata/private matrix; handler guard paths incl. cache hit + already-claimed) and **live-verified** end-to-end against local Postgres: real external fetch of example.com → thin-content degrade recorded durably, SSE `status`+`complete` replay, preview prefill, cache/rate-limit/SSRF/CSRF-split all firing through the full middleware stack; `go build ./...` + `go test ./...` green. Follow-ups belong to their own queued items: the publish gate (next), the before/after UI + share OG image, headless fallback (v1.1), and unclaimed-asset/import GC scheduling.
- [x] Publish gate: re-spin-originated drafts (`generation_jobs.respin_import_id is not null`) require L2 claim before first publish — one guard in the existing publish handler (Spec 17 divergence). **Done: `respin.Service.IsRespinOriginatedSite(siteID)` detects origin via `exists(select 1 from generation_jobs where site_id = $1 and respin_import_id is not null)`; the publishing handler gained an optional consumer-side `RespinOriginChecker` seam (`WithRespinGate`, no import of respin — `*respin.Service` satisfies it structurally, injected in `server.go` from `s.database.(respin.DB)`). The publish route checks it after authorization/site-limit and before `service.Publish`: an originated site published by a session without a claimed identity returns `403 identity_required`; a gate DB error fails closed (500). "Claimed identity" is the new `auth.Session.HasClaimedIdentity()` = `IsAuthenticated() || IsClaimed()` (Spec 17 L2 — authenticated login or a claimed trial whose owner added an email); generation, editing, and preview stay ungated. The builder maps `identity_required` to the existing `claim` blocked-action mode ("Save your workspace / Add an email"), reusing the claim UI. Covered by Go handler tests (unclaimed-trial → 403 identity_required + Publish never called; authenticated → 200; claimed-trial → 200; non-respin site with unclaimed trial → 200 no-op; gate error → 500) and a real-Postgres integration test (`TestIsRespinOriginatedSite`: respin-linked site true, prompt-originated site false, blank id false). `go build ./...`, `go test ./...`, `npm run web:lint`, `npm run web:build` green; live-verified the API boots with the gate wired and the publish route mounted.**
- [x] Demo UI — **after-only per the 2026-07-12 descope** (Spec 21 updated; no source screenshot, no headless infra in v1): `/respin` URL input → progress view (poll/SSE on the status endpoint) → after-view rendering the draft preview with the source URL as a plain new-tab link for self-comparison → "Re-spin another" + "Sign up to keep it" claim modal; degraded imports drop the salvaged `promptPrefill` into the ordinary homepage prompt flow with honest messaging. **Done: new `apps/web/src/routes/respin.tsx` implements the full state machine (input → running → after / degraded / error) against typed API helpers in `lib/api.ts` (`startRespin`, `getRespinStatus`, `getRespinPreview`, `claimRespin`, `respinErrorCode` + `RespinStatus`/`Respin*Response` types, all `credentials`-correct so the public start carries no session and the claim adopts the demo cookies). The progress view polls the durable status endpoint every 1.5s (cache-hit/replaced imports land terminal on the first poll) and renders a three-step stepper mapped from the `fetching`/`extracting`/`composing` statuses; the after-view reuses the existing preview-token → `getPreviewDraft` → `SiteDraftRenderer` path (identical to `/preview/$token`) behind a sticky action bar with the source URL as a plain new-tab link, "Re-spin another" (reset), and "Sign up to keep it" (claim modal → `claimRespin` → `/app/sites/$siteId`). Hard-degraded imports (no generated draft) show an honest "head start" panel whose CTA drops the salvaged `promptPrefill` into the ordinary anonymous-session homepage flow (`startAnonymousSession` → `/app?prompt=…`), exactly the landing form's path. Start errors are mapped by structured code (`invalid_url`/`rate_limited`/`respin_busy`) to friendly copy. Fully localized: ~40 native `respin.*` strings added to both `is`/`en` catalogs in `lib/i18n.ts` (TypeScript-enforced parity, catalog-parity tests green), and a discoverable "Already have a website? Re-spin it" entry link added to the landing hero. The shared warm dark palette was extracted to `lib/landing-theme.ts` and reused by both surfaces. `npm run web:lint`, `npm run web:build`, and the full 88-test vitest suite are green. **Live-verified end-to-end against the local API + Postgres via Playwright:** a content-rich site ran queued→extracting→composing→succeeded and rendered the generated draft in the after-view; "Sign up to keep it" adopted the demo session and landed in the builder with the trial banner showing "1/25 prompts used" (claim books exactly one prompt per Spec 21) and the re-spin publish-gate "Claim this workspace before you publish" copy; a thin-content site (`example.com`) degraded to the head-start panel with the salvaged prefill; no console errors on the re-spin surface (the builder's `form-submissions`/`auth/refresh` 401s are pre-existing trial-panel behavior).
- [ ] Shareable artifact: frozen after-view share page by slug with a static branded OG image. The composed before/after OG image lands with P2b (headless capture, ADR 0004); GTM Facebook-group launch posts use manually captured before/afters until then.
- [ ] Unclaimed-import GC: schedule `respin.Service.DeleteUnclaimedBefore` on the cache TTL and cascade the demo trial workspace + ingested assets. The store method exists (`internal/respin/service.go:315`) but nothing invokes it — flagged as a follow-up in the endpoints item but never queued; without it every abandoned demo accumulates a workspace, a generated site, and pulled assets forever.
- [ ] The 50-site Icelandic QA loop (operational, Spec 21): run re-spin against real Icelandic sites, grade, fix blocks/prompts. Gates the Phase 1 launch and feeds the first vertical block set (Spec 23 — blocked on choosing the vertical, an open product decision).

## P2b — Re-spin v1.1: Headless Capture Service (promoted 2026-07-13, ADR 0004)

Promoted out of Deferred: four workstreams converge on one capability — the before/after source screenshot, the composed OG money shot (the GTM Facebook-group centerpiece), the fetch fallback for JS-walled sites, and later computed-style brand-color sampling. Architecture is decided (ADR 0004, Spec 21 Headless Capture Service section): a stock browserless/chromium container in a **separate Railway project** (Railway private networking spans a project, so the separate project is the isolation boundary — rendered attacker JS has no private-mesh path to api/postgres), called synchronously from the respin pipeline over token-authenticated HTTPS. Runs after the P2a fidelity items; does not gate the 50-site QA loop's start (v1 after-only behavior remains the degradation path).

- [ ] Deploy the capture service: browserless/chromium image in its own Railway project — `TOKEN`, `MAX_CONCURRENT_SESSIONS=2`, `TIMEOUT=25000`, downloads disabled, ~1–2 GB RAM. No custom code.
- [ ] Go capture client behind `CAPTURE_URL`/`CAPTURE_TOKEN` env vars (document in `.env.example`): `Screenshot(ctx, url)` → `POST /screenshot` (PNG) and `RenderedHTML(ctx, url)` → `POST /content`, ~30s client timeout, `ValidatePublicURL` pre-check on every call, capture failure = soft degradation, never a failed import.
- [ ] Wire the headless fetch fallback (Spec 21 pipeline step 3): when plain fetch yields insufficient content, fetch rendered HTML through the capture client instead of degrading straight to the prompt flow; record `fetch_mode='headless'` (the constant and CHECK value already exist as forward-compat markers).
- [ ] Before/after view + composed OG image (unblocks the P2 shareable-artifact item): capture the source screenshot during the pipeline, store it via the asset path with import provenance, upgrade the share page to the frozen before/after and serve the composed side-by-side OG image.
- [ ] Computed-style brand color sampling (after the P2a stylesheet item ships — this supersedes its heuristics when capture is available): read the rendered primary button/`h1` computed styles for ground-truth brand color; keep the static chain as the plain-fetch path.

## P3 — Landing Trust Layer (founder-led)

Moved below re-spin 2026-07-10: the founder wants to handle this manually, and it must not block the re-spin work. Content for the Icelandic landing surface:

- [ ] EU-hosted claim — verify residency before claiming it (Spec 13: "verifiable, not copy"): confirm the production Railway services and Postgres/object storage actually run in an EU/EEA region and the LLM provider processes data under EU-compatible terms; `.env.example` still defaults `S3_REGION=us-east-1`.
- [ ] "Built and personally checked by one developer you can reach" section (GTM guardrail 6 — keep your face on it).
- [ ] GDPR clarity: plain-language landing statement of what data is collected and where it lives (not the deferred Icelandic legal pages).
- [ ] Accessibility statement (WCAG 2.1 AA by-default direction).

## P4 — Carried-Forward Correctness (verified still open 2026-07-03)

- [ ] Magic-link anti-enumeration remainder: existing-account failure paths return 500 while unknown emails get 200 (auth/handler.go:1001–1043 vs :417), and the email-keyed rate limit only runs after existence is confirmed.
- [ ] Tenant integrity composite constraints — all five still absent across 25 migrations (block/page/site, entry/collection/site, page/collection/site, asset/workspace/site, published-version/site).
- [ ] Public URL correctness remainder: site-slug change never invalidates published artifacts (sites/handler.go:278–307 doesn't call `InvalidateSite`); no URL remapping when a custom domain is removed while published.
- [ ] Analytics correctness remainder: SSR loaders don't forward the visitor User-Agent (`publicAPIRequest`, api.ts:771–782 — Node UA reaches crawler filtering); collection-entry views are recorded under the template page's ID.
- [ ] Asset lifecycle: alt/delete APIs exist but no UI calls them; no rename in the API; no usage surface; `Delete` does no reference check and the S3-then-DB sequence can orphan.
- [ ] Custom-domain truthful state machine: only active/pending exist; no TLS/failed states, no cert ownership tracking. (The downstream .is/ISNIC guidance in Spec 13 is deferred — see Deferred.)
- [ ] Collection block contracts remainder: `collection-picker` and number/boolean editor controls fall through to text inputs; `showFilters`/`showSort` props never read by the renderer; block versioning is a passthrough no-op.
- [ ] Inline authoring keyboard/touch accessibility: block selection is click-only, 28px touch targets, no aria-live save status, no focus traps in drawer/inserter/picker/toolbar menus.
- [ ] Billing/email acceptance coverage remainder: no tests for payment-failed/cancellation/downgrade/portal webhooks, 5 of 8 templates untested, no Mailpit round-trip or Resend error-classification tests.

## P5 — Hardening and Product Depth (all verified still open)

- [ ] Builder loading resilience: single `Promise.all` at app.sites.$siteId.index.tsx:473 fails the whole builder if any panel call fails.
- [ ] Draft-preview form semantics: preview-token pages submit forms live; decide authenticated draft path vs disabled state with copy.
- [ ] Expose "published with draft changes" (no comparison logic exists anywhere).
- [ ] Caching/artifact hardening: unbounded in-memory publish cache, `cdnPurger` never wired, no partial-S3-upload cleanup.
- [ ] Atomic rate-limit admission: count-then-insert races in auth and forms limiters.
- [ ] Complete block versioning: `type@version` dispatch, real migrations, historical snapshot rendering.
- [ ] Generation progress lifecycle: no job cancellation endpoint; imagery step ordering; persistent background completion feedback.
- [ ] Extend initial generation to structured content (collections/entries/bindings in the first generation pass — currently only the standalone draft-from-prompt endpoints). The P2a re-spin collections item builds the `GenerateInput.SeedCollections` seam, the deterministic index/detail page templates, and the planner awareness; this item adds the prompt-path collection-worthiness decision (likely an outline-stage LLM judgment, unlike re-spin's deterministic thresholds) on top of that machinery.
- [ ] Trial/email UX remainder: "Already have an account" and resend affordances, standardized exhausted-trial CTA metadata, first-generation education decision.
- [ ] Inquiry management: search, pagination, spam signals, export (export overlaps the GTM CSV-export escape hatch — pull forward if Phase 1 customers ask).
- [ ] Decompose oversized modules (builder route 4,781 lines; generation service 2,358; openai adapter 1,584; publishing service 1,477).

## P6 — Cleanup

- [ ] Remove placeholder `internal/pages`, `internal/workspaces`, `internal/blocks` modules and their 501 routes (api/server.go:220, 238–239).
- [ ] Purge stale `UNSPLASH_*` / `RAILWAY_API_TOKEN` from `.env.example` (already gone from Go config); the Stripe price var rename lands with the P1 billing item.
- [ ] Record billing/domain audit events: `auditRecorder` is injected but never called in billing (service.go:65,118); domains has no audit at all. Spec 15 now formally requires the billing events.

## Spec Debt

- [ ] Reconcile Specs 01–03 with shipped behavior: clarification interview, iterative AI workflow, header/nav chrome, trial publishing (the GTM weave added forward-looking scope but did not backfill these).
- [ ] Spec 20: document the shipped collection/entry history scope chips.
- [ ] Reconcile collection-template slug/uniqueness rules across Specs 05/06/12.
- [ ] Backfill Spec 06 from migrations 000021–000025 (page-identity preservation, collection schema versioning) and the upcoming locale/respin migrations.
- [ ] Spec 13: document the actual TLS/proxy/wildcard/forwarded-host operational contract (the GTM weave added ISNIC/EU notes only).
- [ ] Spec 17/19: verify trial-banner and collection-runtime behavior are fully reflected.

## Won't Do

- Footer business-registration localization: the Spec 04 `businessId` prop (kennitala rendered as "Kt." for Iceland, org.nr reserved for Sweden) and its JSON-LD emission. Decided 2026-07-10; removed from Specs 04/22 and the GTM checklists. Owners who want a registration number in the footer can put it in ordinary footer copy.

## Deferred (demand-pulled, per GTM guardrails — do not pre-build)

- Sweden phase: `sv` locale, SEK prices, Swish, du-form tone rules, the rendered-sites WCAG 2.1 AA spec formalization, Fortnox.
- `.is` domain guidance (deferred 2026-07-10): the ISNIC delegation-aware verify step and Icelandic help copy (Specs 13 + 22). Custom domains keep the generic flow regardless of TLD for now.
- Payment methods beyond standard Stripe checkout (deferred 2026-07-10): no wallet enablement (Apple/Google Pay), Swish, Netgíró, Klarna, or Aur work — plain Stripe cards is the payment story until demand pulls otherwise.
- Ops layer beyond CSV export: pipeline view, booking, follow-up drafts.
- ~~Headless fetch for JS-walled sites, the "before" source screenshot + composed before/after OG image~~ — promoted 2026-07-13 to P2b with a decided architecture (ADR 0004). Still deferred: re-spin language toggle on the preview.
- Privacy/data-protection spec, multi-site Pro marketing, larger-LLM tier.
- Icelandic legal-page translation (lawyer review).
- Founding-50 cap mechanics — blocked on the open GTM decision in [GTM.md](GTM.md).
