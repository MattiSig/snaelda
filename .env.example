# Copy this to `.env` for shared defaults. Put machine-specific overrides in `.env.local`.
# Load precedence for local Go commands is: process env > .env > .env.local.

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5-mini

# Unsplash
UNSPLASH_APPLICATION_ID=your_unsplash_application_id_here
UNSPLASH_ACCESS_KEY=your_unsplash_access_key_here
UNSPLASH_SECRET_KEY=your_unsplash_secret_key_here

# Pexels (starter imagery for generation; leave blank to disable)
PEXELS_API_KEY=your_pexels_api_key_here

# Google Analytics 4 for the snaelda.io marketing/app surface (leave blank to
# disable; never loaded on customers' hosted sites). Read at runtime by the
# web server, so set it on the Railway web service, not at build time.
GA_MEASUREMENT_ID=

# Railway (project token, used by the railway CLI; not read by the app)
RAILWAY_TOKEN=your_railway_project_token_here

# App
APP_ENV=development
HTTP_ADDR=:8080
APP_BASE_URL=http://localhost:3000
PUBLIC_BASE_URL=http://localhost:3000
PUBLIC_BASE_DOMAIN=localhost
API_BASE_URL=http://localhost:8080
VITE_APP_BASE_URL=http://localhost:3000
# Toggle the silent hero demo on the landing page. Keep unset/false in
# production until the real captured assets land at
# apps/web/public/media/hero-demo/.
VITE_HERO_DEMO_ENABLED=false
OPERATOR_EMAILS=
EMAIL_TRANSPORT=stdout
EMAIL_FROM_ADDRESS=hi@snaelda.app
EMAIL_FROM_NAME=Snaelda
EMAIL_REPLY_TO=
RESEND_API_KEY=
MAILPIT_SMTP_ADDR=localhost:1025
AUTH_JWT_SECRET=replace-with-a-long-random-secret
AUTH_ISSUER=snaelda-api
AUTH_AUDIENCE=snaelda-web
AUTH_ACCESS_TOKEN_TTL=15m
AUTH_REFRESH_TOKEN_TTL=720h
AUTH_COOKIE_SECURE=false
PUBLISHED_ARTIFACTS_DIR=var/published-artifacts
# Storage backend for published artifacts. "s3" persists to the S3-compatible
# bucket used by the asset pipeline (recommended); "local" writes to disk under
# PUBLISHED_ARTIFACTS_DIR. Defaults to "s3" when S3_BUCKET is configured.
PUBLISHED_ARTIFACTS_BACKEND=s3
# Optional override for the bucket holding published artifacts. Defaults to S3_BUCKET.
PUBLISHED_ARTIFACTS_S3_BUCKET=
# Optional key prefix to namespace published artifact objects in the bucket.
PUBLISHED_ARTIFACTS_S3_PREFIX=published-artifacts

# Stripe billing
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
STRIPE_PRICE_BASIC=price_replace_me
STRIPE_PRICE_PRO=price_replace_me
STRIPE_PRICE_ONCE_OVER=price_replace_me
BILLING_SUCCESS_URL=http://localhost:3000/app/billing/success
BILLING_CANCEL_URL=http://localhost:3000/app/billing/cancel
BILLING_PORTAL_RETURN_URL=http://localhost:3000/app/billing

# Database
DATABASE_URL=postgres://user:password@localhost:5432/snaelda?sslmode=disable

# Storage
S3_ENDPOINT=http://localhost:8333
S3_BUCKET=snaelda-local
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=snaelda
S3_SECRET_ACCESS_KEY=snaelda-secret
S3_FORCE_PATH_STYLE=true

# Local SeaweedFS port overrides
SEAWEEDFS_S3_PORT=8333
SEAWEEDFS_MASTER_PORT=9333
SEAWEEDFS_FILER_PORT=8888
